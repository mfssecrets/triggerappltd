<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Required on Android 14+ for the incoming-call notification to bypass
         the lock screen and show IncomingCallScreen as a full-screen intent. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Required on Android 14+ when the foreground service hosts an active
         call session (keeps the WebRTC peer connection alive while the app
         is backgrounded — TODO: not used yet, needed when real audio is added). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />

    <!-- Required to start a foreground service on API 26+ (Oreo). Without this,
         startForegroundService() throws SecurityException at runtime. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Required on Android 14 (API 34) for foregroundServiceType="dataSync". -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon_round"
        android:supportsRtl="true"
        android:theme="@style/App.Splash"
        tools:targetApi="31">

<!--        android:extractNativeLibs="false"-->
        <activity
            android:name=".main.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/App.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- foregroundServiceType="dataSync" is required on Android 14+ when
             startForeground() is called with FOREGROUND_SERVICE_TYPE_DATA_SYNC. -->
        <service
            android:name=".notifications.ReplyService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <service
            android:name=".notifications.UnreadMessagesService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Firebase Cloud Messaging service for push notifications -->
        <service
            android:name=".notifications.TriggerMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <receiver android:name=".notifications.ReplyNotificationReceiver" android:exported="false"/>

        <activity
            android:name="com.canhub.cropper.CropImageActivity"
            android:theme="@style/CropImageTheme"/>

<!--        <service android:name=".network_moniter.NetworkService"/>-->
    </application>
</manifest>
